From RocqSched Require Export Operational.Awkernel.MinimalProjection.

(** Thin Awkernel-facing entry point for the multicore operational
    projection bridge. Downstream refinement lemmas should import this file
    rather than depending on the minimal wrapper directly. *)
